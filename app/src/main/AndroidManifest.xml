<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.smallblack.myfirstapplication">
    <uses-sdk android:minSdkVersion="4" android:targetSdkVersion="15" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DisplayMessageActivity"></activity>

        <!--<activity android:theme="@android:style/Theme.Dialog"></activity>-->
        <!--<activity android:theme="@android:style/Theme.Translucent"></activity>-->

        <!--custom theme defined in /res/values/styles.xml-->
        <!--<activity android:theme="@style/CustomTheme"></activity>-->
    </application>

</manifest>